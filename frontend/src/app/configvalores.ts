export class Configvalores {
    constructor(vent: boolean, inund: boolean, temp_max: number, temp_min: number,
        hum_max: number, hum_min: number, luz_max: number, luz_min: number,
        co2_max: number, co2_min: number) {}
}
